## Задание 1

n = int(input())
a = [0]*n
b = [0]*n
temp = 0

print("Введите массив А.")
for i in range(n):
    a[i] = int(input())
print("Введите массив В.")
for i in range(n):
    b[i] = int(input())
    
for i in range(n):
    temp = a[i]
    a[i] = b[i]
    b[i] = temp

print(a, b)
    
